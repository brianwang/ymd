# Tasks

- [x] 1. 盘点“我的/个人信息”页的未开发点并确定交互与信息架构
  - [x] 1.1 明确菜单项行为：我的活动、我的订单、编辑个人资料、积分中心、邀请海报、登录/绑定/退出
  - [x] 1.2 明确资料字段：头像、昵称、邮箱、积分（沿用现有字段与接口）

- [x] 2. 小程序端：美化“我的/个人信息”页并补齐菜单交互
  - [x] 2.1 优化视觉：信息卡、账户卡、菜单列表的层级/间距/触控反馈与现有主题一致
  - [x] 2.2 将“我的活动/我的订单/编辑个人资料”改为可点击跳转
  - [x] 2.3 保持未登录/未绑定邮箱时的引导逻辑不变，但呈现更清晰

- [x] 3. 小程序端：实现“编辑个人资料”页（昵称 + 头像）
  - [x] 3.1 新增页面与路由：编辑页支持返回与保存
  - [x] 3.2 昵称表单：校验（2–20、去空白）、保存调用 `PUT /api/v1/users/me`
  - [x] 3.3 头像选择与上传：调用 `POST /api/v1/media/upload` 获取 URL 后更新 `PUT /api/v1/users/me`
  - [x] 3.4 保存成功后更新 `userStore.userInfo` 并返回上一页

- [x] 4. 后端：活动与报名最小可用 API（支撑“我的活动”）
  - [x] 4.1 Alembic 迁移：创建 `events` 与 `event_registrations` 表（与现有模型字段一致）
  - [x] 4.2 新增公开活动 API：`GET /api/v1/events`、`GET /api/v1/events/{id}`（仅已发布）
  - [x] 4.3 新增报名 API：`POST /api/v1/events/{id}/registrations`、`DELETE /api/v1/events/{id}/registrations/me`
  - [x] 4.4 新增我的报名 API：`GET /api/v1/users/me/event-registrations`（返回含活动信息的列表）
  - [x] 4.5 业务校验：截止时间、容量、重复报名、已发布约束

- [x] 5. 小程序端：实现“我的活动”页并联动活动详情
  - [x] 5.1 新增页面与路由：展示我报名的活动列表（含空态、下拉刷新）
  - [x] 5.2 取消报名：确认弹窗 + 调用取消接口 + 列表刷新
  - [x] 5.3 活动详情联动：在活动详情页展示报名状态，并支持报名/取消（用于形成闭环验证）

- [x] 6. 小程序端：实现“我的订单”页（空态 + 引导）
  - [x] 6.1 新增页面与路由：订单列表布局与空态展示
  - [x] 6.2 引导入口：提供可点击按钮跳回活动/共居（不依赖订单后端）

- [x] 7. 验证与回归
  - [x] 7.1 后端：`alembic upgrade head` 成功；新增 API 可用（手测或脚本）
  - [x] 7.2 小程序端：个人信息页所有菜单项可点击且行为明确；资料编辑可保存并回显
  - [x] 7.3 小程序端：我的活动可显示/可取消；活动详情可报名/取消
  - [x] 7.4 `ymd-app` 与 `ymd-server` 的现有检查（如 type-check）通过

# Task Dependencies
- 3 依赖 2（入口与样式先统一）
- 5 依赖 4（需要活动/报名 API）
- 7 依赖 2/3/4/5/6
