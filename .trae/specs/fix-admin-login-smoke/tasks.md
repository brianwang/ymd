# Tasks

- [x] 1. 定位后台无法登录原因并形成可复现结论
  - [x] 1.1 明确 ymd-admin 使用的登录接口与 token 存储方式（baseURL、路径、字段名）
  - [x] 1.2 核对服务端登录校验链路（密码哈希算法、用户查询条件、禁用/启用、错误码）
  - [x] 1.3 对照当前提供的后台账号：是否存在、是否是 superuser、密码是否可校验

- [x] 2. 修复后台管理员账号的生成与交付
  - [x] 2.1 修复/补齐测试账号生成脚本：确保创建 `is_superuser=true` 且包含 email/hashed_password
  - [x] 2.2 更新 `test_accounts.md`：补充可用的后台管理员账号（邮箱/明文密码/用途）
  - [x] 2.3 增加幂等性：重复执行脚本不会产生重复管理员账号或破坏密码校验

- [x] 3. 修复 ymd-admin 登录联调与错误反馈（最小变更）
  - [x] 3.1 确认 API baseURL 配置（`VITE_API_BASE_URL`）在本地/测试环境可用
  - [x] 3.2 登录失败时展示可定位的错误信息（401/403/网络错误区分）
  - [x] 3.3 登录成功后携带 token 访问一个管理接口作为“会话有效”校验

- [x] 4. 增加联调冒烟验证（最小自动化）
  - [x] 4.1 增加一个脚本/测试用例：使用管理员邮箱/密码登录并请求 `/api/v1/admin/users`（或等价存在的管理接口）
  - [x] 4.2 增加一个脚本/测试用例：使用非管理员 token 访问同一接口应返回 403
  - [x] 4.3 将脚本输出与失败信息设计为可快速定位（打印 HTTP 状态码与关键响应片段，不输出敏感 token 全量）

# Task Dependencies
- 3 依赖 2（需要确定可用管理员账号）
- 4 依赖 2/3（需要可用账号与稳定联调链路）
