- [x] 后台管理员账号在数据库中存在：`email`、`hashed_password` 均有效，且 `is_superuser=true`
- [x] `test_accounts.md` 中的后台账号可用（按文档操作可登录）
- [x] ymd-admin 使用管理员账号可成功登录并进入后台（token 生效）
- [x] 管理接口对管理员返回 200（至少验证一个 `/api/v1/admin/*`）
- [x] 管理接口对非管理员返回 403（至少验证同一个接口）
- [x] 联调冒烟脚本/测试可一键运行并在失败时给出可定位的输出（不泄露 token）
