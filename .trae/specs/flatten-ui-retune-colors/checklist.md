- [ ] V2 tokens 的阴影策略已扁平化（默认无阴影或极弱），且不会导致页面层级丢失
- [ ] V2 tokens 的主色/强调色已更新，小程序端整体色调统一且对比度足够
- [ ] `Card`/`EmptyState` 默认样式已扁平化（边框/背景/圆角一致），无“凸起感”
- [ ] 关键页面在扁平化后仍清晰：积分中心/社区/活动/共居/我的
- [ ] `ymd-app`：`npm run type-check` 通过
- [ ] `ymd-app`：`npm run build:h5` 通过

