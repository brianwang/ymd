# Tasks

- [x] 1. 定义主题与 token（小程序端 + 后台端）
  - [x] 1.1 小程序端：在 `uni.scss` 定义颜色、圆角、阴影、字号、间距 token，并让页面引用这些 token
  - [x] 1.2 后台端：在 `ymd-admin/src/style.css` 定义同等 token（CSS 变量），并让视图使用变量

- [x] 2. TabBar 图标与配置
  - [x] 2.1 设计并生成 4 组 Tab 图标（普通态/选中态）：活动、社区、共居、我的
  - [x] 2.2 将图标写入 `ymd-app/src/static/tab/`，并更新 `pages.json` 的 `tabBar.list[*].iconPath/selectedIconPath`
  - [x] 2.3 调整 tabBar 文字色与选中色对齐新主题

- [x] 3. 页面视觉一致性改造（小程序端）
  - [x] 3.1 活动页/共居页：卡片、筛选 chips、banner 更换为主题风格；补齐空态/加载态/错误态的视觉一致性
  - [x] 3.2 社区页/详情/发帖页：统一背景、卡片与按钮色；优化图片栅格展示边距与圆角
  - [x] 3.3 我的页/积分中心/邀请海报：统一卡片、按钮、分割线与文字层级；优化“任务中心”区域观感

- [x] 4. 资源图集（banner/空态插画/占位图）
  - [x] 4.1 生成首页/活动页 banner（2–3 张）
  - [x] 4.2 生成空态插画（列表为空/网络错误）至少 2 张
  - [x] 4.3 将资源写入 `ymd-app/src/static/`（以及必要时 `ymd-admin/src/assets/`），并替换页面引用

- [x] 5. 验证
  - [x] 5.1 小程序端与后台端分别 `npm run type-check` 通过
  - [x] 5.2 小程序端 Tab 图标显示正常，切换不抖动；页面无明显色彩冲突
  - [x] 5.3 H5 构建通过（小程序端与后台端）

# Task Dependencies
- 2 依赖 1（颜色与选中色统一）
- 3 依赖 1/4
- 5 依赖 1/2/3/4
