- [ ] 用户可设置/更新偏好位置；未设置时所有列表保持原默认行为
- [ ] 活动：支持 `near_lat/near_lng` 距离排序；`radius_km` 半径过滤正确；启用时返回 `distance_km`
- [ ] 活动推荐：前端不再使用 `slice(0,6)` 生成推荐；推荐来源可解释且可复用
- [ ] 社区（帖子）：支持发布时可选写入位置；列表支持距离排序/半径过滤；无位置帖子有稳定降级策略
- [ ] 共居页：种子空间数据包含 `lat/lng`；已设置位置时“附近推荐”按距离变化；未设置时不报错且保持原展示
- [ ] 数据库迁移可回滚；新字段均可空；不影响现有登录、发帖、评论、活动报名主流程
- [ ] 覆盖基础测试（经纬度校验、距离排序/过滤），并通过现有 CI/本地测试命令

