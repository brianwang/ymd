- [x] 已明确需要 AI 生成的测试图片类型、尺寸、数量，并记录在资产清单中
- [x] 测试图片资产目录约定清晰，前端/后端引用路径一致且稳定
- [x] seed/mock 仅通过资产清单选择图片，不再散落硬编码路径或临时 URL
- [x] AI 生成脚本支持幂等（默认不覆盖）、并发/限流配置、失败提示清晰
- [x] AI 接口鉴权信息不写入仓库（仅环境变量/本地配置）
- [x] 未配置 AI 接口时可降级运行（仍可完整跑通 seed/mock 与页面展示）
- [x] 完成一次端到端验证：生成 → 落盘 → seed/mock 引用 → 前端/后台正确显示
