- [x] 积分中心「任务中心」三行按钮在同一对齐基准线上（同一右边界或同一中心线），不同文案长度不影响对齐
- [x] 任务按钮高度/内边距/字号一致；primary/ghost/disabled 状态不改变布局尺寸导致抖动
- [x] 余额卡片头部左右信息基线对齐；昵称超长时省略且不挤压数值区域
- [x] 积分流水分值列右对齐且最小宽度稳定；正负数混排不跳动
- [x] `ymd-app`：`npm run type-check` 通过
- [x] `ymd-app`：`npm run build:h5` 通过
