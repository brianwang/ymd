- [x] posts 表新增 `location` 与 `tags` 列，迁移可正常 upgrade
- [x] 创建帖子接口接收并返回 `location` 与 `tags[]`，且不破坏既有 `media[]/image_urls` 兼容
- [x] 服务端可从正文解析 `#标签`，与显式 tags 合并去重归一化；非法 tags 返回 4xx + 可读错误
- [x] 信息流与详情接口返回 `location` 与 `tags[]`，前端可正确展示
- [x] `/api/v1/media/upload` 覆盖录音常见格式与大小限制，语音录制产物可上传
- [x] 发帖页支持：录制语音/上传语音、选择/移除位置、添加/删除标签；校验仍为“文字或 media 至少一项”
- [x] 至少手测通过一条“带语音 + 标签 + 位置”的发帖与展示（信息流预览 + 详情页播放）
